{% extends 'base.html.twig' %}

{% block title %}Ajout d'une adresse{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-6 m-5">
            <div class="card">
                <div class="bg-info p-1 text-white"><i class="fas fa-user-edit fa-2x"></i> Ajout d'une adresse</div>
                <div class="card-body">
                    {{ include('address/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
