<div class="row">
    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="col-md-6">
        <section class="panel">
            <div class="panel-body">
                <div class="form-group">
                    {{ form_label(form.gender, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.gender) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.gender, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.firstname, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.firstname) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.firstname, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.lastname, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.lastname) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.lastname, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.matricule, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.matricule) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.matricule, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.email, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.email) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.username, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.username) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.username, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
            </div>
        </section>
    </div>

    <div class="col-md-6">
        <section class="panel">
            <div class="panel-body">
                <div class="form-group">
                    {{ form_label(form.phone1, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.phone1) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.phone1, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.phone2, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.phone2) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.phone2, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.entity, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.entity) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.entity, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.mainFunction, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.mainFunction) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.mainFunction, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.birthdate, null, {'label_attr': {'class': 'col-md-4 control-label'}}) }}
                    {{ form_errors(form.birthdate) }}

                    <div class="col-md-8">
                        {# Génération de l'input. #}
                        {{ form_widget(form.birthdate, {'attr': {'class': 'form-control datepicker'}}) }}
                    </div>
                </div>
            </div>
        </section>
    </div>
    <div class="col-md-12">
        <footer class="panel-footer">
            <button class="btn btn-primary">
                <i class="fa fa-check-square" aria-hidden="true"></i>
                {{ button_label|default('Enregistrer') }}
            </button>
            <a class="btn btn-warning" href="javascript:history.go(-1)">
                <i class="fa fa-circle-o-notch" aria-hidden="true"></i>
                Annuler
            </a>
        </footer>
    </div>
    {{ form_end(form) }}
</div>
